<?php

namespace App\Management\Functions\Master;

/**
 * ユーザーID編集
 *
 */
class UserIdEdit
{
    /**
     * main
     *
     * @return bool
     */
    public function main(): bool
    {
        //マイページ画面が必要
        //      ユーザーIDの編集ができる
        //      編集後にIDをユニークチェックする機能が必要
        //      保存するボタンが必要
        //          保存ボタン押下時にIDをユニークチェックする機能が必要
        $isname = false;
        if ($isname) {
            //同じ名前がある
            //入力ボックスの下に赤字でメッセージを出す機能が必要
        } else {
            //同じ名前ではない
            //変更したユーザーIDを保存する機能が必要
        }

        return false;
    }
}
